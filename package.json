{
  "name": "email-api",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "typescript": "^4.6.4",
    "wrangler": "2.0.5"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "zod": "^3.16.0"
  }
}
